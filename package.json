{
  "name": "job-hunter-bot",
  "version": "3.1.0",
  "description": "Cloudflare Worker — polls RSS feeds for remote jobs, matches skills via multi-layer scoring, alerts via Discord/Telegram",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "build": "echo 'No build step required for Worker'",
    "dev": "wrangler dev",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --testEnvironment=node",
    "test:watch": "node --experimental-vm-modules node_modules/.bin/jest --watch"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "wrangler": "^4.69.0"
  },
  "keywords": [
    "job",
    "bot",
    "cloudflare-worker",
    "remote",
    "rss",
    "discord",
    "telegram"
  ],
  "author": "Papu",
  "license": "ISC"
}